# cook your dish here
import sys
def solve():
    N, M = map(int, input().split())
    X, Y = map(int, input().split())
    res = (N * X) + (M * Y)
    print(res)
if __name__ == "__main__":
    try:
        solve()
    except (EOFError, ValueError):
        pass
        