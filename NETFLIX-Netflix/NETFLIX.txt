# cook your dish here
import sys
def solve():
    A,B,C, X = map(int, input().split())
    
    if A + B >= X or B + C >= X or A + C >= X:
        print("YES")
    else:
        print("NO")
        
if __name__ == "__main__":
    try:
        T = int(input())
        for _ in range(T):
            solve()
    except(EOFError, ValueError):
        pass